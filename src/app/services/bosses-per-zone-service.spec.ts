import { BossesPerZoneService } from './bosses-per-zone-service';

describe('BosszoneDal', () => {
  it('should create an instance', () => {
    expect(new BossesPerZoneService()).toBeTruthy();
  });
});
