import { CharactersConfigurationS } from './characters-configuration-service';

describe('TeamDal', () => {
  it('should create an instance', () => {
    expect(new CharactersConfigurationS()).toBeTruthy();
  });
});
