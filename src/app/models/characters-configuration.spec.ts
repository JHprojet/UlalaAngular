import { CharactersConfiguration } from './characters-configuration';

describe('CharactersConfiguration', () => {
  it('should create an instance', () => {
    expect(new CharactersConfiguration()).toBeTruthy();
  });
});
